export enum PageName {
  HOME = 'home',
  AUTH = 'auth',
  RACES = 'races',
  DRIVERS = 'drivers',
  SEARCH = 'search',
  NOT_FOUND = 'notfound',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark'
}
