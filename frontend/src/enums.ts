export enum PageName {
  HOME = 'home',
  RACES = 'races',
  DRIVERS = 'drivers',
  SEARCH = 'search',
  AUTH = 'auth',
  NOT_FOUND = 'notfound',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark'
}
